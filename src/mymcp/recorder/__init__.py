"""mymcp recorder — optional async module that maintains a server overview.

Disabled by default. Enable with MYMCP_RECORDER_ENABLED=true. No extra
install needed — LLM calls go through httpx, a core dependency.

Public symbols are added as later tasks land.
"""

__all__: list[str] = []
